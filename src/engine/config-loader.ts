// Engine config loader — stub for Phase 2
// Discovers and loads shiftwork.config.ts from the workspace

export async function discoverConfig(_cwd?: string): Promise<string | null> {
	// Stub — will search for shiftwork.config.ts up the directory tree
	return null;
}
